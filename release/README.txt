FlowFetch Linux x86_64 Release

This package contains a single-file FlowFetch executable for Linux x86_64.

Quick start:

  chmod +x flowfetch
  ./flowfetch --help
  ./flowfetch <url>

Example:

  ./flowfetch https://example.com/demo.zip

This release does not require Python or pip installation in advance.
